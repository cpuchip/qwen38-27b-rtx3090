Arm 3 second-round independent review, seat astra

Reviewed checkout `7cb63d1b99c6c8619352efe604c37aeb9c97c534`, both first-round reviews, and the requester's response. Citations are physical file lines, including diff markers, not installed-engine line numbers. Installed code, the rebuilt counter, and its runtime output are unavailable. Their behavior is UNVERIFIED. The retired backport is historical evidence only: it explicitly excludes the current stack, and the Docker build skips it (`patches/dflash2-backport.patch:1-2`; `Dockerfile:28-33`).

1. **A. The revised design still needs changes before inferential paired cells. Smoke and counter development can proceed in the stated order.**

   **The experimental unit has not been repaired by multiplying seeds.** The number of boots is unspecified. The switch is read during initialization and supplied as the selector's constexpr, so this implementation assigns treatment at boot, not separately to each request (`patches/dflash2-lookup-drafting.patch:287-291`, `455-474`). If the 64 pairs all compare one on boot with one off boot, there is still one paired boot contrast. Prompt-seed differences can describe that contrast, but cannot separate treatment from boot or estimate between-boot uncertainty. Disabling lookup removes a particular source of state dependence; it does not randomize boot effects. The pilot half-width of 0.40 does not validate a power calculation for independent boot contrasts.

   Register the number of independently initialized A/B boot pairs, randomize or counterbalance their A/B order, and freeze warmup, cache/kernel policy, request schedule, and measurement windows. For example, allocating eight paired prompt rows to each of eight boot pairs uses 16 treatment boots and yields 64 row pairs, but only eight independent boot-pair contrasts. Alternatively, repeat the full 64-row schedule across independent boot pairs. Analyze paired boot summaries or an appropriate clustered design, with precision based on boot replication, not 63 residual degrees of freedom. Neither example is a guarantee of power. Estimate paired boot-level variance and size for the registered equivalence interval inside +/-0.15 tok/step. If inference is meant to generalize beyond these eight prompts, prompt sampling is another limitation; eight seeds do not create eight new prompts. With one boot pair, label the result descriptive and conditional on those boots.

   **Seven draft positions are supported conditionally by the fork, not established by a successful health check.** The launcher maps `DFLASH_TOKENS` into `num_speculative_tokens` (`single-user/start_qwen.sh:231-232`). The patch starts `draft_block` at `num_speculative_steps` and clamps only with lookup enabled; query count becomes `1 + draft_block` (`patches/dflash2-lookup-drafting.patch:46-71`). Selector launch length, candidate reshaping, and the copied token block all use `draft_block` (`patches/dflash2-lookup-drafting.patch:469`, `503-524`). Thus, conditional on the unavailable upstream configuration resolving `num_speculative_steps=7`, lookup off gives seven draft positions, indexed 0-6, and eight query tokens including the bonus. It does not give seven selector candidates: that dimension is separately `selector_top_k` (`patches/dflash2-lookup-drafting.patch:273-279`, `469-470`).

   With these equal widths, `_adaptive` is false and `next_num_draft_tokens()` returns None; the handler retains the supplied token width when no override is given (`patches/dflash2-lookup-drafting.patch:336-339`, `398-399`, `933-936`). The convolution independently clamps its block against checkpoint configuration, so the actual checkpoint must also be checked for the expected eight-token query block (`patches/dflash2-lookup-drafting.patch:20-23`). Smoke must generate and verify tokens and print the resolved configuration, tensor shapes, and actual per-position submission counts. Installed configuration resolution, checkpoint dimensions, and successful execution remain UNVERIFIED.

   Use the canonical `VLLM_DFLASH2_LOOKUP=0`: the patch reads that name; `LOOKUP=0` is translated only by the launcher, which rejects conflicting aliases (`patches/dflash2-lookup-drafting.patch:57`, `294`; `single-user/start_qwen.sh:203-218`). Preserve explicit probabilistic draft sampling as well as request sampling parameters. The checked-in DFlash configuration still omits `draft_sample_method`, and the visible selector gate depends on a live draft-logit buffer, attached top-p state, and nonzero temperature (`single-user/start_qwen.sh:231-232`; `patches/dflash2-lookup-drafting.patch:203`, `455-474`). A printed request payload alone cannot establish those gates.

   **The counter must be demonstrated under graph replay.** Fixing flattened position alignment is necessary but insufficient. The patch explicitly says Python inside the draft path executes at CUDA graph capture, and the chain patch implements its live host state outside that captured path (`patches/dflash2-lookup-drafting.patch:389-392`; `patches/dflash2-ngram-chains.patch:34-43`, `120-123`). A Python print inserted between the two calls is not proof of one observation per serving pass. Require a replayed GPU measurement or snapshot at that location, with completed records drained outside capture, preserving boot, request, pass, and position identity. Reconcile records against live draft passes; exclude capture/profile/warmup records. The submitted counter implementation is UNVERIFIED. Per-pass synchronization would also require a separate assessment before using those runs for timing.

   The two exact-count controls need qualified inputs. The mask uses strict-greater ranks and cumulative mass, so ties spanning rank eight can retain more than eight entries, and top-p can further reduce the count (`patches/dflash2-lookup-drafting.patch:207-215`). Use a synthetic, moderate, distinct-score, 16-finite-candidate row: T=1, top-p=1, top-k=20 should retain 16; change only top-k to 8 for the exact-eight check. A live top-p=1 row is not an unconditional 16-finite-entry guarantee either: the code has no top-p=1 bypass, and floating-point cumulative mass can round to one for negligible tails (`patches/dflash2-lookup-drafting.patch:209-215`). Compare live counts with a reference predicate and record original finite count and boundary ties before declaring the counter broken. Test truncation off and an active request occupying a nonzero state slot too; the batch/state distinction in C matters.

   **The replacement position prediction is a falsifiable descriptive claim only after its statistic is specified. It does not identify the cause of -28 accepted tokens.** Define whether "smallest" and "rising" refer to every pass, a median, or a mean, and whether rising must be strict at every adjacent position. A workable registration is a mean kept count per position within each prompt-seed row, then a prespecified equal-weight summary within each boot, with contrasts for position zero versus positions 1-6 and the claimed adjacent ordering. Define a practical flatness tolerance and uncertainty rule in advance. Failure to detect a slope is not evidence of flatness. Evaluate all eligible generated positions, and separately report those submitted and reached in verification. The selector loops over the configured draft positions, while its mask contains no target probabilities or acceptance outcomes (`patches/dflash2-lookup-drafting.patch:186-188`, `203-215`, `469`).

   The corrected fixed-context identity is

   `alpha(p,q_S) - alpha(p,q) = G - L`,

   where `G = sum_on_S [min(p_i,q_i/Z) - min(p_i,q_i)] >= 0` and `L = sum_off_S min(p_i,q_i)`. Kept count measures neither G nor L. Removing one token with useful overlap can lose more acceptance than removing many tokens with almost no target overlap. A flat kept-count curve therefore falsifies the registered count ordering, if established with adequate precision, but does not falsify support-removal loss. Conversely, the predicted ordering does not prove that loss outweighed the retained-support gain. "Most peaked" needs a defined concentration measure; higher maximum probability or lower entropy alone does not guarantee a strictly smaller 0.95 nucleus.

   If attributing a loss to this mechanism, measure removed q mass and, on matched contexts where p is available, removed overlap and retained gain. Free-running A/B trajectories need not share p. Report accepted counts with their denominators: a raw -28 can reflect different verification-round counts or exposure. Register mean-of-row ratios versus pooled counts; the existing benchmark computes request `1 + accepted/rounds` and pools counts for its total, which gives a different weighting (`bench/labd_bench.py:140-142`, `148-160`). Finally, the lookup-off experiment estimates a different regime from the earlier production loss. Without production exposure and matched-context evidence, it cannot retrospectively establish why that position lost 28 tokens.

2. **B. The claimed consumer behavior is consistent with the patch contract, but closure is not independently established.**

   **Established in the current patch:** `DFlash2Speculator.draft_logits_spec()` returns `(torch.float32, -inf)`; the return is retained context beneath the moved override, not a return of zero (`patches/dflash2-lookup-drafting.patch:372-375`, `439-442`). The selector uses temperature-scaled scores for the mask, replaces excluded original logits with -inf, and stores those original masked logits as `realized` (`patches/dflash2-lookup-drafting.patch:203-215`, `225-232`). The stated contract is pre-temperature cached logits (`patches/dflash2-lookup-drafting.patch:439-441`).

   **Only partially exposed for the cache:** the current cache hunk loads previous candidate IDs and changes their addressing to use the full verification width (`patches/dflash2-lookup-drafting.patch:235-253`), but its diff context omits the subsequent erase/write stores. The complete historical kernel explicitly erases old IDs to -inf, writes new scores, and updates the candidate-ID cache (`patches/dflash2-backport.patch:853-870`). That is consistent with the requester's account, but this retired implementation is not proof of the installed 0.28 kernel (`patches/dflash2-backport.patch:1-2`). The current lookup point-mass kernel does visibly erase old entries, write the selected token at logit zero, and replace the candidate-ID list with that token, preserving the bookkeeping needed to erase it on the next ordinary draft pass (`patches/dflash2-lookup-drafting.patch:799-821`).

   **UNVERIFIED:** the upstream base-class default of 0.0, the allocation actually honoring the override, the installed ordinary cache kernel's erase-before-write implementation, and the claimed `rejection_sampler_utils.py:165-184` global logsumexp and `draft_logit/temp - draft_lse`. Those installed lines cannot be checked here. The fork's earlier validation record reports the opposite temperature finding, so the requester should retain the exact installed source excerpt and engine identity that supersede it (`docs/v0.28-validation.md:697`). Neither report is source verification in this checkout.

   Conditional on correct allocation, complete erasure, the stated consumer formula, and matching sampling temperature/support, vocabulary-wide normalization gives exactly `q_S(i) = exp(score_i/T) / sum_on_S exp(score/T)`. Negative-infinity entries contribute zero. There is no contradiction between a global denominator and normalization on the kept support. The call passes masked scores and the probabilistic temperature to the Gumbel helper, but its implementation and agreement with verification remain UNVERIFIED (`patches/dflash2-lookup-drafting.patch:217-232`).

   The patches specify concrete failure signatures if the installed account is false. Initializing untouched vocabulary entries to zero would violate the override's sparse-cache contract and give them positive softmax mass (`patches/dflash2-lookup-drafting.patch:439-442`). Failing to erase old finite entries would leave support outside the current candidate set; compare the historical erase at `patches/dflash2-backport.patch:866` and the current lookup bookkeeping at `patches/dflash2-lookup-drafting.patch:817-821`. Normalizing against an unmasked denominator would not produce normalized q on the retained support, because the stored row already contains -inf for removals (`patches/dflash2-lookup-drafting.patch:215`, `225-232`). Omitting draft temperature scaling would disagree with the visible pre-temperature contract at nonunit T (`patches/dflash2-lookup-drafting.patch:207`, `221`, `227`, `439-442`). None of these contradictions proves the unseen consumer correct. Keep the mechanistic cells at explicit T=1 and obtain an installed row-to-q comparison; a nonunit-T diagnostic is needed to distinguish the competing temperature accounts.

3. **C. On the ordinary selector path, the proposed location precedes lookup replacement. That alone does not validate every observation made there.**

   The exact current-patch order is `_sample_path` at line 518, conditional `_cache_draft_logits` at lines 519-520, copying selector tokens at lines 521-523, then `_apply_lookup` at line 524 (`patches/dflash2-lookup-drafting.patch:518-524`). The cache helper launches `_cache_draft_logits_kernel` at line 486 (`patches/dflash2-lookup-drafting.patch:478-497`). These are the inspectable call sites; installed lines 672 and 728 remain UNVERIFIED. The ordinary point-mass rewrite is inside `_apply_lookup`, after its early return, suffix search, and token fusion (`patches/dflash2-lookup-drafting.patch:532-585`).

   A correctly ordered device hook there can inspect `_selector_scores`, the float32 `[batch row, draft position, selector candidate]` buffer containing the walked candidate logits after truncation. Its allocation, selector argument, and stores are visible (`patches/dflash2-lookup-drafting.patch:273-279`, `464-465`, `225-232`). It can alternatively inspect the freshly cached `draft_logits` rows in request-state/vocabulary coordinates, conditional on the cache implementation established only partially in B (`patches/dflash2-lookup-drafting.patch:245-253`, `486-496`). The selector buffer is not the full candidate-transition score tensor and is not normalized q; the walked logits are what gets stored (`patches/dflash2-lookup-drafting.patch:190-215`, `225-232`).

   Do not use a request-state ID directly as a selector-buffer batch index. `sample_idx_mapping` maps batch positions to request-state slots; its stride here is `draft_block` (`patches/dflash2-lookup-drafting.patch:537-540`, `841-842`). Use it to identify valid batch rows when reading `_selector_scores`, and to translate into request-state rows when reading `draft_logits`. Read only freshly written draft positions. The cache spans `num_speculative_steps`, while selector storage and ordinary writes span `draft_block`, so a longer production verification tail is not current truncation output at this hook (`patches/dflash2-lookup-drafting.patch:248-250`, `268-285`, `493-494`, `518-524`). Also, token IDs in `_selector_tokens` are samples, not distributions: one ID per position does not mean point-mass q (`patches/dflash2-lookup-drafting.patch:218-234`, `464-465`).

   **No earlier lookup point-mass rewrite of the current ordinary selector output is visible. A singleton support can nevertheless already be present.** With a unique best candidate, top-k=1, or top-p satisfied by that candidate alone, the truncation predicate can leave one finite logit before caching; normalizing it gives a point mass without any lookup involvement (`patches/dflash2-lookup-drafting.patch:203-215`, `225-232`). By contrast, passing temperature zero to the sampler does not itself rewrite the stored selector logits to one-hot: the stored value remains `scores` (`patches/dflash2-lookup-drafting.patch:221-232`). When `draft_logits` is absent, the cache call is skipped altogether, so there is no fresh cached q to count (`patches/dflash2-lookup-drafting.patch:519-520`).

   There is also an earlier point-mass path that bypasses this hook completely: engaged chains return `_chain_generate` before `super().propose`, and that path fills tokens from lookup and invokes the point-mass kernel (`patches/dflash2-ngram-chains.patch:161-179`, `318-344`). It does not first produce selector output. Explicit chains off excludes that path; the patch also disables chains when lookup is off (`patches/dflash2-ngram-chains.patch:44-47`). A later ordinary draft must erase any prior lookup/chain point mass via the candidate cache, subject to B's installed-kernel limitation. Capture-safe observation and live-row validation are still required as described in A. Pre-lookup counts in production establish selector activation only, not the fraction surviving replacement into verification.

VERDICT: proceed with changes: retain smoke then controls; specify and replicate boot-level treatment assignment, validate the counter under graph replay with qualified count and mapping controls, verify the live seven-position layout, and register a precise position statistic before inferential paired cells; installed consumer closure remains UNVERIFIED.
